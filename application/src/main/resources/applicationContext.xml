<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:aop="http://www.springframework.org/schema/aop"
         xmlns:tx="http://www.springframework.org/schema/tx"
         xsi:schemaLocation="http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
           http://www.springframework.org/schema/aop 
           http://www.springframework.org/schema/aop/spring-aop-2.0.xsd
           http://www.springframework.org/schema/tx 
           http://www.springframework.org/schema/tx/spring-tx-2.0.xsd">

    <!-- the parent application context definition for the springapp application -->

    <bean id="productService" class="springapp.service.SimpleProductService">
        <property name="productDao" ref="productDao"/>
    </bean>

    <bean id="productDao" class="springapp.repository.JpaProductDao" />

<!-- 
	JPA mod: dataSource is defined in repositoryContext.xml
	
    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>
 
    <bean id="propertyConfigurer" 
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:jdbc.properties</value>
            </list>
        </property>
    </bean>
 -->

<!--
	JPA-Mod: Defined in repositoryContext.xml
	
    <bean id="transactionManager" 
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
  -->
  
  <!-- 
  	JPA Mod: transactions are now explicit with @Transactional
    <aop:config>
        <aop:advisor pointcut="execution(* *..ProductService.*(..))" advice-ref="txAdvice"/>
    </aop:config>


	     [Keith] I had to change the method name in the first tx:method tag from "save*"
	     to "increasePrice". Otherwise the increase-price transaction fails because
	     the method name matches only the second pattern, forcing it to be read-only.
	     However, the downstream method wants to update the Product table, so it fails.

	 
    <tx:advice id="txAdvice">
        <tx:attributes>
            <tx:method name="increasePrice"/>
            <tx:method name="*" read-only="true"/>
        </tx:attributes>
    </tx:advice>
 -->
 
</beans>